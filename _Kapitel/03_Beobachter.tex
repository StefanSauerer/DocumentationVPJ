\chapter{Beobachter}
Wie in dem Blockschaltbild (ref einfügen) zu sehen wird am Ende nicht die aktuelle Positon des Roboters sondern eine Verzögerte Position gemessen. Diese Verzögerung entsteht durch die Verarbeitung der Kammera-Messwerte sowie der Versendung über MQTT. Zudem kommt ein neuer Messwert immer nur alle 100 ms (Noch prüfen, ob wir wo anders eine Erklärung/Messung dafür anbringen) an, was zu einem Treppenförmigen Signal führt.
\section{Zielsetzung}
Zur Behebung dieser zwei Probleme soll ein Beobachter verwedet werden. Dieser nimmt als Eingang die Raddaten des Roboters und soll uns dann zu jeder Zeit aus der Anfangsposition und den Umdrehungen der Räder, die Aktuallposition des Roboters Vorwärtspropagieren.\\
Um dies zu ermöglichen müssen zwei Dinge passieren. Zuersteinmal werden immer die Raddaten der letzten 100ms benötigt, um die Verzögerung der Kamera auszugleichen. Das heißt bei eintreffen eines neuen Messwerts muss die Erhaltene Position um diese Zeitspanne vorwärtberechnet werden. Parallel läuft dauerhaft das Kinematische Modell mit, welches aus der Anfangsposition und den Raddaten kontinuierlich eine Geschätzte Position berechnet. Diese beiden Werte können dann miteinander verglichen werden, um den Fehler zu berechnen und mit einem Gain den Beobachterkreis zu schließen. Dadurch nähert sich schlussendlich der Beobachter und seine Zustandsgrößen dem realen Roboter an.
% -------------------------------------------------------------------------------- %
% Kinematisches Beobachtermodell
% -------------------------------------------------------------------------------- %
\section{Kinematisches Beobachtermodell}
Der Roboter verfügt über drei omnidirektionale Räder, die in einem Winkel von $\alpha_1 = 60^\circ$, $\alpha_2 = 180^\circ$ und $\alpha_3 = 300^\circ$ angeordnet sind.
Jedes Rad erzeugt eine Geschwindigkeit entlang seiner Rollrichtung sowie eine Querkomponente, die durch die Kopplung mit den beiden anderen Rädern entsteht.\\
Wie in Abbildung \ref{fig:Grafik-Omniroboter} zu sehen, sind die Geschwindigkeitsvektoren der Räder tangential zum Kreis des Roboters angeordnet und entsprechen den jeweiligen Radgeschwindigkeiten $V_{w1}$, $V_{w2}$ und $V_{w3}$.\\
\begin{figure}[H]
	\centering
	\begin{tikzpicture}[scale=2]

	% Mittelpunkt
	\coordinate (O) at (0,0);

	% Radius
	\def\L{1.2}

	% Radpositionen (deine Winkel)
	\coordinate (W1) at (60:\L);
	\coordinate (W2) at (180:\L);
	\coordinate (W3) at (300:\L);

	% Roboterkreis
	\draw[thick] (O) circle (\L);

	% Räder
	\fill (W1) circle (0.03) node[above right] {$w_1$};
	\fill (W2) circle (0.03) node[left] {$w_2$};
	\fill (W3) circle (0.03) node[below right] {$w_3$};

	% Radiuslinien
	\draw[dashed] (O) -- (W1);
	\draw[dashed] (O) -- (W2);
	\draw[dashed] (O) -- (W3);

	% Lokales Koordinatensystem
	\draw[->,thick] (O) -- (0,1.6) node[left] {$y_l$};
	\draw[->,thick] (O) -- (1.6,0) node[below] {$x_l$};

	% Geschwindigkeitsvektoren (tangential zu Kreis)
	\draw[->,red] (W1) -- ++(-0.4,0.25) node[above] {$V_{w1}$};
	\draw[->,red] (W2) -- ++(0,-0.5) node[left] {$V_{w2}$};
	\draw[->,red] (W3) -- ++(0.4,0.25) node[below] {$V_{w3}$};

	% Mittelpunktbeschriftung
	\node at (O) [below left] {$M$};

	% Radiusbeschriftung
	\draw[<->] (0,-1.4) -- (\L,-1.4);
	\node at (0.6,-1.55) {$L$};

	\end{tikzpicture}
	\caption{3-Rad-Omniroboter mit Radwinkeln $60^\circ$, $180^\circ$, $300^\circ$}
    \label{fig:Grafik-Omniroboter}
\end{figure}
Um die Gesamtgeschwindigkeit des Roboters zu berechnen, müssen die Beiträge aller drei Räder berücksichtigt und in das globale Koordinatensystem transformiert werden.Hierfür wird zunächst die Geschwindigkeit jedes Rades im lokalen Radkoordinatensystem $(x_j, y_j)$ beschrieben, wobei die erste Zeile die Querkomponente und die zweite Zeile die direkte Rollkomponente darstellt. Die Querkomponente entsteht durch die Kopplung mit den anderen Rädern und lässt sich berechnen, indem die Geschwindigkeitsvektoren der anderen Räder entsprechend ihrer Anordnung und der Geometrie des Roboters gewichtet werden. Da die Räder in einem Winkel von 120° zueinander angeordnet sind, ergibt sich für die Querkomponente ein Faktor von $1/\sqrt{3}$\textcolor{red}{(Vielleicht hier einfach Quelle angeben und nicht erklären?)}:

\begin{equation}
    \vec{V}_1^{wheel} = 
    \begin{bmatrix}
    \frac{V_{w3}}{\sqrt{3}} - \frac{V_{w2}}{\sqrt{3}} \\
    V_{w1}
    \end{bmatrix}
\end{equation}
\\
\begin{equation}
    \vec{V}_2^{wheel} = 
    \begin{bmatrix}
    \frac{V_{w1}}{\sqrt{3}} - \frac{V_{w3}}{\sqrt{3}}\\
    V_{w2}
    \end{bmatrix} \\
\end{equation}
\\
\begin{equation}
    \vec{V}_3^{wheel} = 
    \begin{bmatrix}
    \frac{V_{w2}}{\sqrt{3}} - \frac{V_{w1}}{\sqrt{3}}\\
    V_{w3}
    \end{bmatrix}
\end{equation}

Die zweite Zeile beschreibt die Geschwindigkeit entlang der Rollrichtung des jeweiligen Rades, während die erste Zeile die Querkomponente darstellt, die durch die Kopplung mit den anderen Rädern entsteht.
Diese lokalen Geschwindigkeiten werden dann, über die Rotationsmatrix, in das Roboterkoordinatensystem transformiert:
\textcolor{red}{Hier nochmal überlegen, Wie und ob man Marixes anders darstellt}\\

\begin{equation}
\vec{V}_j = R(\alpha_j)\,\vec{V}_j^{(w)}
\end{equation}

mit der Rotationsmatrix:

\begin{equation}
R(\alpha_j) =
\begin{bmatrix}
\cos\alpha_j & -\sin\alpha_j \\
\sin\alpha_j & \cos\alpha_j
\end{bmatrix}
\end{equation}

Die Gesamtgeschwindigkeit des Roboters ergibt sich als Mittelwert aller drei Räder:

\begin{equation}
\begin{bmatrix}
v_x^{body} \\
v_y^{body} \\
\end{bmatrix}
=
\frac{1}{3} \sum_{j=1}^{3}
R(\alpha_j)\,\vec{V}_j^{(w)}
\end{equation}

Und die Rotationsgeschwindigkeit ist:

\begin{equation}
\dot{\varphi} = \frac{1}{3L}(V_{w1} + V_{w2} + V_{w3})
\end{equation}

Um diese allgemeinen Gleichungen auf den konkreten Roboter anzuwenden, müssen die Winkelwerte $\alpha_1 = 60^\circ$, $\alpha_2 = 180^\circ$ und $\alpha_3 = 300^\circ$ eingesetzt werden.

Durch Einsetzen dieser Werte in die Rotationsmatrizen und Ausmultiplizieren der Summe ergibt sich die folgende kompakte Matrixdarstellung:

\begin{equation}
    \begin{bmatrix}
        v_x^{body} \\
        v_y^{body} \\
        \omega
    \end{bmatrix}
    =
    \begin{bmatrix}
        -\frac{\sqrt{3}}{3} & 0 & \frac{\sqrt{3}}{3} \\
		\frac{1}{3} & -\frac{1}{3} & \frac{1}{3} \\
		\frac{1}{3R} & \frac{1}{3R} & \frac{1}{3R}
    \end{bmatrix}
    \begin{bmatrix}
        V_{\omega1} \\
        V_{\omega2} \\
        V_{\omega3}
    \end{bmatrix}
\end{equation}

Die Umrechnung Geschwindigkeit in Weltkoordinaten erfolgt dabei wiefolgt, wobei $\theta$ der aktuelle Winkel des Roboters im Raum ist:
\begin{equation}
    \begin{bmatrix}
        v_x^{world} \\
        v_y^{world}
    \end{bmatrix}
    =
    \begin{bmatrix}
        \cos(\theta) & -\sin(\theta) \\
        \sin(\theta) & \cos(\theta)
    \end{bmatrix}
    \begin{bmatrix}
        v_x^{body} \\
        v_y^{body}
    \end{bmatrix}
\end{equation}
Die Winkelgeschwindigkeit des Roboters ist in Roboterkoordinaten, als auch Weltkoordinaten gleich definiert, weshlab hier keine Unterscheidung vorgenommen wird.\\

Um schlussendlich die Position im Raum zu berechnen, integriert man die Geschwindigkeiten über die Zeit. Bei einer diskreten Integration mit einem Zeitschritt $\Delta t$ ergibt sich die folgende Formel für die Aktualisierung der Position:
\begin{equation}
    \mathbf{x}_{k+1} =
    \mathbf{x}_k +
    \Delta t \cdot
    \begin{bmatrix}
        v_x^{world} \\
        v_y^{world} \\
        \omega
    \end{bmatrix}
\end{equation}

% -------------------------------------------------------------------------------- %
% Beobachterstruktur
% -------------------------------------------------------------------------------- %
\section{Beobachterstruktur}
Hier vielleicht noch kurz iwas angeben zu, warum Struktur oder so
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{_Bilder/LuneburgerBeobachter_Unbehauen.png}
    \caption{Zustandsbeobachter nach Luneburger \cite{lunze_regelungstechnik1}}
    \label{fig:LuneburgerBeobachter_Unbehauen}
\end{figure}
Die erste Abbildung zeigt die klassische Struktur eines Zustandsbeobachters nach Luenberger. Ausgangspunkt ist ein lineares zeitinvariantes System in Zustandsraumdarstellung mit den Matrizen A, B und C. Ziel des Beobachters ist die Rekonstruktion der nicht direkt messbaren Zustände x auf Basis des Eingangssignals u und der gemessenen Ausgangsgröße y. Der Luenberger-Beobachter bildet dazu ein dynamisches System nach, das strukturell weitgehend identisch mit dem Originalsystem ist. Der zentrale Unterschied besteht in der Rückführung des Schätzfehlers, also der Differenz zwischen gemessenem Ausgang y und geschätztem Ausgang $\hat{y}$
Diese Differenz wird über eine Beobachterverstärkung F (häufig auch L genannt) auf das Beobachtersystem zurückgeführt. Dadurch ergibt sich die typische Beobachterdynamik:
$\dot{\hat{x}}=A\hat{x}+Bu+F(y-\hat{y})$
mit $\hat{y}=C\cdot\hat{x}$.
Die Aufgabe bei der Auslegung des Luenberger-Beobachters besteht darin, die Verstärkungsmatrix F so zu wählen, dass der Schätzfehler $e=x-\hat{x}$ asymptotisch gegen null konvergiert. 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{_Bilder/KalmanFilter.png}
    \caption{Konzept Kalman-Filter}
    \label{fig:KalmanFilter}
\end{figure}
Die zweite Abbildung zeigt das Konzept des Kalman-Filters, das als Erweiterung des Luenberger-Beobachters interpretiert werden kann. Auch hier erfolgt eine Zustandsrekonstruktion, jedoch auf Basis einer stochastischen Beschreibung von Prozess- und Messrauschen. Der Kalman-Filter erweitert die Beobachterstruktur um eine stochastische Beschreibung von Prozess- und Messrauschen. Die Zustandsrekonstruktion erfolgt in zwei Schritten. Dabei wird in der Prädiktion auf Basis des Systemmodells der Zustand sowie die Fehlerkovarianz vorhergesagt:
\begin{align}
\hat{x}_{k+1}^- &= A \hat{x}_k + B u_k \\
P_{k+1}^- &= A P_k A^\top + Q
\end{align}

In der Korrektur wird dann zyclisch die Vorhersage mithilfe der Messung $y_{k+1}$ korrigiert. Der Kalman-Gewinn ergibt sich dabie zu:
\begin{align}
K_{k+1} &= P_{k+1}^- C^\top \left( C P_{k+1}^- C^\top + R \right)^{-1}
\end{align}

Damit werden Zustandsschätzung und Kovarianz aktualisiert:
\begin{align}
\hat{x}_{k+1} &= \hat{x}_{k+1}^- + K_{k+1} \left( y_{k+1} - C \hat{x}_{k+1}^- \right) \\
P_{k+1} &= \left( I - K_{k+1} C \right) P_{k+1}^-
\end{align}
Der Kalman-Filter bestimmt den Verstärkungsfaktor $K_{k+1}$ optimal im Sinne der Minimierung der Schätzfehlerkovarianz. Dabei werden die statistischen Eigenschaften des Prozessrauschens ($Q$) und des Messrauschens ($R$) explizit berücksichtigt.\\
Der wesentliche Unterschied zwischen dem Luenberger-Beobachter und dem Kalman-Filter liegt in der Auslegung der Beobachterverstärkung:
Beim Luenberger-Beobachter wird die Matrix L deterministisch gewählt, typischerweise über Polvorgabe. Rauscheinflüsse werden nicht explizit berücksichtigt. Beim Kalman-Filter ergibt sich der Verstärkungsfaktor als Lösung eines Optimierungsproblems und wird in jedem Zeitschritt neu berechnet. Dabei werden Unsicherheiten systematisch über Kovarianzmatrizen modelliert.\\
\textcolor{red}{Hier noch argumentiren, warum der Kalman-Filter keinen Vorteil bringt. Kammerafehler ändert sich nur wenig usw...\\
Außerdem Kalman teil noch überarbeiten}
% -------------------------------------------------------------------------------- %
% Modell Validierung
% -------------------------------------------------------------------------------- %
\section{Modell Validierung}
Um das Beobachter Modell und dessen Implementierung zu validieren, ist es am einfachsten zuerst die Normalfahrt zu betrachten. Also eine Fahrt in der zyklich neue Kamera-Werte ankommen. Der Verlauf dieser Messwerte ist in Abbildung \ref{fig:Obs_L1_Normalfahrt}, beispielhaft für die x-Position des Roboters, in Blau dargestellt.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{_Grafiken/Obs_L1_Normalfahrt.pdf}
    \caption{Beobachter mit direktem Ausgleich Normalfahrt}
    \label{fig:Obs_L1_Normalfahrt}
\end{figure}
Betrachtet man im Gegensatz zur blauen Kurve die roten, vom Beobachter berechneten Werte und vergleicht diese mit den in der Zielsetzung beschriebenen Anforderungen, lässt sich ein nahezu ideales Verhalten erkennen. Die Position wird um 100ms vorwärts propagiert, und zwischen den Sprüngen wird die Kurve geglättet.\\

Noch deutlicher wird dies, wenn man die Beobachterkurve künstlich um 100ms verzögert. Dies ist in Abbildung \ref{fig:Obs_L1_NormalfahrtVerschoben} dargestellt. Hier ist gut zu erkennen, wie die Kamera bei jedem neuen Messwert wieder exakt auf die kontinuierliche Kurve des Beobachters springt.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{_Grafiken/Obs_L1_NormalfahrtVerschoben.pdf}
    \caption{Beobachter mit direktem Ausgleich Normalfahrt Verschoben}
    \label{fig:Obs_L1_NormalfahrtVerschoben}
\end{figure}
% -------------------------------------------------------------------------------- %
% Regler Parametrierung
% -------------------------------------------------------------------------------- %
\section{Regler Paremetrierung}
\textcolor{red}{Noch Bents Punkt mit dem Ausgleich anbringen (Wo geht des eigentlich genau zurück/ Was sin des für Einheiten)}\\

Für die Regler Parametrierung soll nun eine Fahrt betrachtet werden, in der der Roboter eine längere Zeit keinen Kammera-Messwert bekommt und somit das Modell langsam von der Realität abdriftet. In Abbildung \ref{fig:Obs_L1_Gesammt} ist diese Messung einmal im gesammten dargestellt. Hierfür wurde während der Fahrt in x-Richtung der Navigationscode des Roboters für etwa 12s abgedeckt.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{_Grafiken/Obs_L1_Gesammt.pdf}
    \caption{Beobachter mit direktem Ausgleich Gesammt}
    \label{fig:Obs_L1_Gesammt}
\end{figure}
In Abbildung \ref{fig:Obs_L1_Ausgleich} ist der Zeitberich um den Ausgleich nochmal vergrößert dargestellt. Wie man hier gut erkennen kann, wurde der Gain der Rückführung hier auf eins gesetzt, was besonders durch die schnellen Zycluszeiten des Programms zu einem quasi direkten Ausgleich bei erhalt eines neuen Kammeramesswerts führt.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{_Grafiken/Obs_L1_Ausgleich.pdf}
    \caption{Beobachter mit direktem Ausgleich}
    \label{fig:Obs_L1_Ausgleich}
\end{figure}
Betrachtet man den gleichen Versuch nochmal für einen deutlich geringere Verstärkung, so kann man in Abbildung \ref{fig:Obs_L01_Ausgleich}, für L = 0.01 eine Annäherung an den Kammerawert erkennen.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{_Grafiken/Obs_L01_Ausgleich.pdf}
    \caption{Beobachter mit langsamen Ausgleich}
    \label{fig:Obs_L01_Ausgleich}
\end{figure}
Zu guter letzt soll in Abbildung \ref{fig:Obs_VarGain_Gesammt} nochmal geprüft werden, ob mit einem vom Kamerafehler abhängigen variablen Verstärkung eine weitere Verbesserung des Verhaltens erreicht werden kann. Die Idee dahinter ist ein deutlich vereinfachter Grundgedanke des Kalman-Filters. Wenn der Messwert gut ist, soll sich mehr auf die Kammera verlassen werden und sonst mehr auf das Modell. Da der Kamerafehler direkt mitgeliefert wird ist auch nur noch eine sinnvolle Normierung um diesen Wert einzuführen.\\

\textcolor{red}{Hier noch ne Formel für die Normierung}
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{_Grafiken/Obs_VarGain_Gesammt.pdf}
    \caption{Beobachter mit variablen Gain}
    \label{fig:Obs_VarGain_Gesammt}
\end{figure}
Der Ansatz ist grundsätzlich sinnvoll, zeigt in der Praxis jedoch nur einen geringen Effekt. Dies liegt daran, dass die Kamera bei jeder erneuten Bilderkennung einen sehr ähnlichen systematischen Fehler aufweist. Eine dynamische Korrektur bringt daher nur begrenzte Verbesserungen.
Aus diesem Grund erscheint es zielführender, den Fehler durch Anpassung eines statischen Parameters zu kompensieren. Durch ein gezieltes Tuning dieses festen Wertes kann eine vergleichbare oder sogar stabilere Verbesserung erzielt werden, ohne die zusätzliche Komplexität einer dynamischen Anpassung.